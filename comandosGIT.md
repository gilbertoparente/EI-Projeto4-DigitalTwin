# na branch feature
1. git add .
2. git commit -m "feature pronta"
3. git push origin feature-login-registo

# merge via GitHub (Pull Request)


# depois no Rider
1. git checkout main
2. git pull
1. git checkout -b feature-desktop
2. git push -u origin feature-desktop

